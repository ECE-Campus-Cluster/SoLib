/* database host */
const host = "localhost"

/* database name */
const database = "solib"

/* database username */
const username = "root"

/* database password */
const password = "root"

/* port on wich the server will be listening */
const port = 25000;

module.exports = {
	HOST       : host,
    PORT       : port,
    DBNAME     : database,
    DBUSERNAME : username,
    DBPASSWORD : password
};