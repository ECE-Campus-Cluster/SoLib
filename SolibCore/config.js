/* database host */
const host = "can.vas"

/* database name */
const database = "solib"

/* database username */
const username = "root"

/* database password */
const password = ""

/* port the server will be listening on */
const port = 8080;

module.exports = {
	HOST       : host,
    PORT       : port,
    DBNAME     : database,
    DBUSERNAME : username,
    DBPASSWORD : password
};
